# Environment Configuration for Use Minimal

# Site Configuration
# The base URL for your site (used for sitemap generation and SEO)
SITE_URL=https://useminimal.com

# Analytics (Optional)
# Vercel Analytics token for tracking website usage
NEXT_PUBLIC_VERCEL_ANALYTICS_TOKEN=

# Node Environment
# Should be "development", "production", or "test"
NODE_ENV=production

# Generator Configuration (Optional)
# Path to Puppeteer Chrome executable (if not using system Chrome)
PUPPETEER_EXECUTABLE_PATH=

# Whether to run Puppeteer in headless mode (true/false)
PUPPETEER_HEADLESS=true

# Development Configuration
# Port for the frontend development server (default: 3000)
PORT=3000

# Build Configuration
# Enable bundle analysis (set to "true" to analyze bundle size)
ANALYZE=false